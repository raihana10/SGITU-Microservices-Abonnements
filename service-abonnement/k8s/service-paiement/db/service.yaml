# Exposes the paiement MySQL database inside the SGITU namespace.
apiVersion: v1
kind: Service
metadata:
  name: db-paiement
  namespace: sgitu
spec:
  type: ClusterIP
  selector:
    app: db-paiement
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql

