# Exposes the analytique MongoDB database inside the SGITU namespace.
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: sgitu
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
