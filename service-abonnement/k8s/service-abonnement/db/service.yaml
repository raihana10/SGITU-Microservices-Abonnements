# Exposes the abonnement MySQL database inside the SGITU namespace.
apiVersion: v1
kind: Service
metadata:
  name: db-abonnement
  namespace: sgitu
spec:
  type: ClusterIP
  selector:
    app: db-abonnement
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql

