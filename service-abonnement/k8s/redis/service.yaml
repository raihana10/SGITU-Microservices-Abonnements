# Exposes Redis inside the SGITU namespace.
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: sgitu
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis

