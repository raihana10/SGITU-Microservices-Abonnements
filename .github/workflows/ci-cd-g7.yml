name: CI/CD - G7 Suivi Véhicule

on:
  push:
    branches: [ "main", "develop" ]
    # Déclenche uniquement si des fichiers dans le microservice G7 sont modifiés
    paths:
      - "service-suivi-vehicule/**"
      - ".github/workflows/ci-cd-g7.yml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "service-suivi-vehicule/**"

jobs:
  code-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: service-suivi-vehicule
    steps:
    - name: Récupérer le code
      uses: actions/checkout@v4

    - name: Configurer Java 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Analyser la qualité du code (Warnings)
      run: mvn clean compile -Dmaven.compiler.showWarnings=true -Dmaven.compiler.compilerArgs=-Xlint:all

  build-and-test:
    runs-on: ubuntu-latest
    
    # Toutes les étapes s'exécutent dans le dossier du microservice
    defaults:
      run:
        working-directory: service-suivi-vehicule

    steps:
    - name: Récupérer le code
      uses: actions/checkout@v4

    - name: Configurer Java 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven' # Met en cache les dépendances Maven pour accélérer les builds suivants

    - name: Compiler le projet
      run: mvn clean package -DskipTests

    - name: Exécuter les tests unitaires
      run: mvn test -Dspring.profiles.active=test

    - name: Publier les rapports de test Surefire
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: surefire-reports
        path: service-suivi-vehicule/target/surefire-reports/

  docker-build:
    needs: [build-and-test, code-quality]
    runs-on: ubuntu-latest
    # Ne s'exécute pas sur les PR, uniquement sur les pushs validés
    if: github.event_name == 'push'
    
    permissions:
      contents: read
      packages: write # Nécessaire pour publier sur GHCR

    steps:
    - name: Récupérer le code
      uses: actions/checkout@v4

    - name: Convertir le nom du dépôt en minuscules
      run: |
        echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

    - name: Configurer Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Connexion au registre GitHub (GHCR)
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Construire et publier l'image
      uses: docker/build-push-action@v5
      with:
        context: ./service-suivi-vehicule
        file: ./service-suivi-vehicule/Dockerfile
        push: true
        # Utilisation de la variable convertie en minuscules
        tags: |
          ghcr.io/${{ env.REPO_LOWER }}/g7-suivi-vehicules:latest
          ghcr.io/${{ env.REPO_LOWER }}/g7-suivi-vehicules:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
